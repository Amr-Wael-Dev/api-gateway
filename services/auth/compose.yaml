services:
  auth-service:
    build:
      context: ../..
      target: dev
      args:
        SERVICE_PATH: services/auth
    ports:
      - "3002:3002"
    environment:
      - NODE_ENV=development
      - MONGO_URI=mongodb://admin:password@mongo:27017/auth-db?authSource=admin
      - REDIS_URL=redis://redis:6379
    env_file:
      - .env
    depends_on:
      - mongo
      - redis
    networks:
      - backend
    develop:
      watch:
        - action: sync
          path: ./src
          target: /app/services/auth/src
        - action: rebuild
          path: ./package.json
